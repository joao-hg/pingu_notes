import 'package:flutter/material.dart';

class AppColors {
  // Primary brand accent (mockup green)
  static const primaryGreen = Color(0xFF22C55E);
  static const primaryGreenDark = Color(0xFF16A34A);

  // Light mode surfaces
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF9FAFB);
  static const cardBorder = Color(0xFFE5E7EB);
  static const ink = Color(0xFF111827);
  static const mutedInk = Color(0xFF6B7280);
  static const paper = Color(0xFFFFFFFF);

  // Dark mode surfaces (near-black, matching mockup)
  static const darkBackground = Color(0xFF0D1117);
  static const darkSurface = Color(0xFF161B22);
  static const darkCard = Color(0xFF1C1C2E);
  static const darkBorder = Color(0xFF30363D);

  // Status / feedback
  static const success = Color(0xFF22C55E);
  static const danger = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);

  // Category icon colors (matching mockup)
  static const categoryStudy = Color(0xFF8B5CF6);  // purple
  static const categoryProject = Color(0xFF22C55E); // green
  static const categoryAudio = Color(0xFF3B82F6);   // blue
  static const categoryAI = Color(0xFFEC4899);      // pink/magenta
  static const categoryNote = Color(0xFFF59E0B);    // amber/yellow
  static const categoryTask = Color(0xFF14B8A6);    // teal

  // Legacy palette (kept for backward compatibility)
  static const iceBlue = Color(0xFFD6ECF7);
  static const deepOceanBlue = Color(0xFF0D3B66);
  static const softWhite = Color(0xFFF8FAFC);
  static const warmYellow = Color(0xFFFFD166);
  static const softOrange = Color(0xFFFF9F66);
}
